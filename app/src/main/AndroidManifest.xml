<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pocketteam.pocketteam" >

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".WelcomeActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AddTeamActivity"
            android:label="@string/add_team"
            android:parentActivityName=".TeamListActivity" />
        <activity
            android:name=".AddPlayerActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".RosterActivity"
            android:label="@string/app_name"
            android:parentActivityName=".TeamListActivity" />
        <activity
            android:name=".TeamListActivity"
            android:label="@string/pocketTeam"
            android:parentActivityName=".WelcomeActivity" />
        <activity
            android:name=".PlayerProfileActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".SprayChartActivity"
            android:label="@string/app_name" />

        <activity android:name=".BattingAvgActivity" >
        </activity>
        <activity android:name=".EraActivity" >
        </activity>
        <activity android:name=".ScoutPlayerActivity" >
        </activity>
        <activity android:name=".HitsActivity" >
        </activity>
        <activity android:name=".SluggingActivity" >
        </activity>
        <activity android:name=".RbiActivity" >
        </activity>
        <activity android:name=".HomeRunActivity" >
        </activity>
    </application>

</manifest>
